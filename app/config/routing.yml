pilotabai_competition_db:
    resource: "@PilotabaiCompetitionDbBundle/Controller/"
    type:     annotation
    prefix:   /

pilotabai_competition_db_api:
    resource: "@PilotabaiCompetitionDbBundle/Controller/Api"
    type:     annotation
    prefix:   /
    defaults:
        _format: json

fos_user:
    resource: "@FOSUserBundle/Resources/config/routing/all.xml"

app:
    resource: "@AppBundle/Controller/"
    type:     annotation
